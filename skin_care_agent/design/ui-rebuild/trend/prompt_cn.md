请根据我提供的最终参考图，实现“历程”模块的 3 个页面。

参考图是本次任务的视觉标准，请尽量忠实还原，不要自行发挥，不要重做风格，不要套用框架默认样式。

【任务范围】

实现 / 调整以下 3 个页面：

1. 历程（主页面 / 皮肤地图入口页）
2. 左脸颊 · 这一段记录（时间轴详情页）
3. 左脸颊 · 对比观察（对比模式页）

这 3 个页面需要使用统一的视觉语言。

【整体风格要求】

整体气质应当是：
- 安静
- 温暖
- 克制
- 有档案感
- 护肤产品气质明确
- 精致但不过度装饰

不要让装饰成为主角。
页面的美感应主要来自：
- 字体层级
- 留白
- 线条质量
- 卡片比例
- 精致的组件本身
- 柔和的材质感

不要增加过多背景花纹。
不要额外堆很多花草、贴纸或装饰图案。
允许非常轻微的视觉点缀，但信息组件本身必须始终是视觉重点。

【颜色 / 材质方向】

以暖米白 / 纸面感为基础，搭配柔和的绿色点缀。

建议方向：
- 暖米白背景
- 暖纸面色卡片
- 柔和低饱和绿色选中态
- 深色主文字
- 灰一点的次级文字

卡片质感应当是：
- 哑光
- 纸面感
- 轻微悬浮（如果需要）
- 低对比
- 不发亮
- 不是玻璃拟态

避免：
- 大面积纯白
- 很硬的黑色描边
- 很重的阴影
- 发亮的玻璃感
- 强渐变

【页面 1：历程（主页面）】

这是模块的主页面，所以必须有底部导航栏。

页面名称：
- 必须是“历程”
- 不能写成“皮肤地图”

顶部区域：
- 标题：历程
- 标题下有简短副标题
- 右上角设置图标
- 有“全脸 / 分区”的切换
- 参考图中“分区”为选中状态

主视觉：
- 以脸部地图作为核心入口
- 尽量复现你当前满意的那版脸谱实现
- 不要重新发明一套新的脸部插画风格

脸部绘制要求：
1. 整体线稿更统一
2. 区域形状更自然
3. 区域内部留白更均衡
4. 选中态更明确
5. 区域框选更精细

区域设计原则：
- 不要过度贴合真实解剖边界
- 区域边界要干净、安静、便于交互
- 实际点击热区可以比视觉边界略大
- 保留参考图中的区域逻辑

选中态：
- 使用柔和的绿色填充
- 选中区域比未选中区域更清晰、更好识别
- 同时保持柔和，不要太跳

脸谱下方：
- 有“正在记录”模块
- 用轻量列表展示正在记录的区域
- 不要在这个列表里展示真实用户照片
- 直接用圆点或轻量图标即可
- 每个 item 展示：
  - 区域名称
  - 时间点数量
  - 最近日期
  - 右箭头 / 可跳转提示

这个列表是可点击的，点击进入时间轴详情页。
它要看起来干净、有编辑感，不要像默认 Android 列表。

底部导航：
- 主页面必须有
- “历程”为当前激活 tab
- 保持全 App 现有四个 tab 结构

【页面 2：左脸颊 · 这一段记录（时间轴详情页）】

这是二级页面。
不要在这个页面中再嵌入一个三级详情页。

这个页面需要直接展示当前被选中的那一天 / 那条记录的完整内容。

顶部：
- 返回箭头
- 标题：左脸颊 · 这一段记录
- 标题下展示日期范围 + 时间点数量

时间轴部分：
- 多时间点横向时间轴
- 参考图 2 的结构
- 展示不同时间点的皮肤缩略图
- 当前选中的时间点高亮
- 整体要明显体现“随时间变化”的感觉

时间轴下方：
- 直接展示当前选中记录的完整内容
- 不要让用户再点进下一层页面才能看到细节

记录卡片中必须同时包含以下内容，而且它们在同一层级：
1. 照片中可见
2. 你的记录
3. 产品使用记录
4. 来源

重点：
- “产品使用记录”不能像装饰带一样画在卡片外面
- 它必须在主记录卡片内部
- 它和“照片中可见 / 你的记录 / 来源”属于同一层级的信息模块

产品使用记录：
- 视觉上要能看出可以跳转到产品页
- 但也必须在当前页面直接展示内容
- 例如：使用了 阿达帕林凝胶

整个记录卡应当是完整、闭合、自洽的。

【页面 3：左脸颊 · 对比观察（对比模式页）】

这个页面用于直观展示皮肤状态是变好还是变差。

顶部：
- 返回箭头
- 标题：左脸颊 · 对比观察
- 一句辅助文字，说明是在对比两个时间点

对比模式逻辑（本次设计图中）：
- 先假设用户从时间轴中随机选中了两个时间点
- 使用参考图 3 那种并排对比布局即可

主体区域：
- 两张图片并排
- 左边是更早时间点
- 右边是更新时间点
- 每张图下有日期和状态说明

下方：
- 一张“对比观察”总结卡
- 标题：对比观察
- 一句简洁结论，例如：
  - 整体泛红减轻，肤色更稳定。
- 下面可以有几行支持性指标：
  - 泛红程度
  - 肤色状态
  - 整体状态

这个页面要干净、易读，不要过度装饰。

【排版 / 留白 / 组件质量】

请重点关注：
- 大标题优雅
- 次级信息克制
- 正文可读
- 列表行紧凑但有呼吸感
- 卡片圆角柔和
- 对齐整洁
- 图标风格统一
- 分隔方式克制
- 组件节奏平静

整体应当是“精致但低调”。

【禁止事项】

- 不要把“历程”改名成“皮肤地图”
- 不要去掉主页面底部导航
- 不要在时间轴页里再嵌套三级详情页
- 不要把“产品使用记录”放到记录卡外面当装饰带
- 不要在“正在记录”列表里使用真实用户照片
- 不要过度装饰
- 不要使用很重的 Material 默认样式
- 不要使用鲜艳绿色或纯白大块
- 不要重做脸谱风格

【交付要求】

请实现这 3 个页面，并尽量接近我提供的最终参考图。

完成后：
1. 运行页面
2. 截图
3. 与参考图对比
4. 继续修正以下差异：
   - 标题文字
   - 导航栏有无
   - 脸谱还原度
   - 区域形状
   - 留白与间距
   - 卡片结构
   - 时间轴结构
   - 对比模式布局
   - 色调
   - 组件精致度

直到结果与最终方案高度接近再结束。
# 角色
你是资深产品经理 + 经验丰富的互联网运营 + 项目经理。本次任务是做「项目收口前再次review产品设计」，不写代码。

# 项目背景
- 产品：面向痘痘肌用户的垂域 skincare App（MVP 阶段）
- 背景文档：D:\Mia\agent_tryout\skin_care_agent\project_background.md
- MVP 需求规格（唯一验收基准）：D:\Mia\agent_tryout\skin_care_agent\design\product\skin_care_app_mvp_spec.md
- 当前进度记录（可能已过期，需以代码为准校验）：D:\Mia\agent_tryout\skin_care_agent\docs\current_status.md
- 当前项目状态盘点 D:\Mia\agent_tryout\skin_care_agent\docs\wrapup_assessment_2026-09-14.md

# 任务
和我以对话的形式一起评估当前项目还未实现/部分实现的功能是否要加入到MVP版本中

# 任务
产出一份《项目收尾评估与优先级排期》文档，回答三个问题：
1. 现在到哪了？（对照 spec 的整体完成度）
2. 还差什么？（未完成 / 半成品 / 与 spec 不一致 / 待优化）
3. 先做什么？（一条我可以从上往下依次执行的有序清单）

# 执行步骤（请按序进行，并在文档中体现依据）
1. 读 spec，拆出功能模块清单与验收点（这是后续所有对照的骨架）。
2. 通读代码仓库结构，逐模块核对实现状态；重点识别：
   - 只有 UI 没有逻辑 / mock 数据未接真实数据源
   - TODO、FIXME、注释掉的代码、未接线的入口
   - UI 重构后遗留的旧代码、重复实现、断掉的调用链
3. 启动 Android 模拟器实际运行，走一遍主流程，记录：能跑通的路径、报错、崩溃、空白页、跳转死路。
   - 若无法运行（构建失败、缺依赖/密钥等），如实说明卡点，并改为「静态代码审查 + 标注未验证」，不要凭猜测填结论。
4. 交叉比对 current_status.md 与实际代码，明确指出文档中已过期或不准确的描述。

# 输出文档结构
## 1. 结论摘要（≤200 字）
整体完成度百分比（给出估算口径）+ 距离可发布 MVP 的最大障碍 3 条。

## 2. 模块状态总表
表格，字段：模块 | spec 要求 | 实现状态（已完成 / 部分完成 / 未开始 / 与 spec 不符）| 关键证据（文件路径，必要时行号）| 验证方式（实机验证 / 仅代码推断）

## 3. 缺口清单
按模块分组，每条包含：缺口描述 | 类型（功能缺失 / Bug / 与 spec 不一致 / 技术债 / 体验优化）| 影响 | 预估工作量（S/M/L，S≤2h，M≤1天，L>1天）| 依赖项

## 4. 优先级排期（核心产出）
有序编号列表，从 1 开始，我将按此顺序执行。
排序规则（冲突时按此优先级裁决）：
1) 阻塞主流程 / 导致崩溃的问题最优先
2) MVP spec 中的必需功能 > 增强功能 > 优化项
3) 被其他任务依赖的前置项前置
4) 同等条件下，工作量小的先做（快速回血、减少上下文切换）
5) 纯体验优化、重构、非必需功能一律排到 MVP 可跑通之后
每条格式：序号 | 任务名 | 所属模块 | 为什么排这个位置（1 句）| 完成标准（可验证的 Done 定义）| 预估工作量
最后给出建议分批：P0「让 MVP 能完整跑通」/ P1「上线前必修」/ P2「上线后优化」。

## 5. 风险与待我确认的问题
- 你无法从代码/文档判断、需要我拍板的决策（≤5 条，用提问形式）
- 你做出的关键假设（如有）

# 约束
- **本次不修改任何代码、不创建/改动业务文件**，只输出文档。
- 文档保存到：D:\Mia\agent_tryout\skin_care_agent\docs\wrapup_assessment_[日期].md（同时在对话中输出全文）
- 中文，简洁，多用表格和短句，不写客套话、不重复我的需求描述。
- 不确定的地方明确标注「未验证 / 需确认」，禁止用推测填充事实。
- 结论必须可追溯：每个判断给出文件路径或运行时现象作为依据。
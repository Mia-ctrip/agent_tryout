# UI rebuild 实现与验收记录

本轮按 `design/skin-care-ui-rebuild-handoff-v1` 继续实施。用户已认可当前视觉效果并要求继续。依照用户明确要求，直接保留当前 `main` 工作区，不创建分支或 worktree，不提交或推送 Git，不覆盖其他工作。

状态：主要页面视觉实现和 Web 回归已完成；原生设备验收尚未完成，不能作为全平台发布通过记录。既有产品目录任务的状态文件未被改写。

## 页面迁移

| 页面/组件 | 已实现 | 保留的行为与边界 |
|---|---|---|
| 共用页面、字体、按钮、导航 | Paper / Sage 配色，细衬线标题、薄分隔线、安全区导航高度、表单零装饰 | 四个 Tab 顺序与入口、表单事件、加载/禁用状态 |
| 观察首页 | 上置标题，不对称主图与圆形辅图，单主按钮、轻量档案列表 | 拍摄、相册、现有区域和最近观察入口 |
| 拍摄/确认 | 对齐单个引导椭圆、快门、原图框、质量状态、区域选择、可选原话 | 原有权限/质量检查/分段/提交/重试状态机；恢复原有多区域选择与完整历史选择，未按示意稿限制到 2 个 |
| 观察结果 | 结论、真实证据与发现、用户原话、后续观察和边界；窄屏自动单栏 | 详细事实仍保留在滚动内容内；不增加医学诊断或因果推断 |
| 历程与区域详情 | 人脸线稿、六区域入口、轻量事件列表、衬线时间点 | 本人真实左右、状态、时间轴、照片隐私与原记录入口 |
| 产品档案与详情 | 低强度瓶叶背景、真实图片、薄线条目、简洁产品详情 | 使用频次排序、单一新增入口、左滑归档、官方说明书和使用记录；不新增商业字段 |
| 登录/注册/同意/账户/新增产品/产品使用 | 无装饰表单、统一标题、焦点边框和错误提示 | 现有输入、校验、API 与操作路径 |

使用 quiet-botanical-ui 约束装饰强度、证据隔离和页面字体；素材来源见 `mobile/assets/brand/ASSET_RIGHTS.md`。正式资源没有复制交接包参考照片或药品包装。截图脚本仅在隔离测试中使用交接包的示例图片，不将其打包进应用。

## 已执行验证

2026-09-07，最终代码检查：

- `npm run test:unit`：211 通过，0 失败。
- `npm run typecheck`：退出码 0。
- `npm run lint`：退出码 0。
- `npx expo export --platform web`：退出码 0，导出 26 个路由到被忽略的 `mobile/dist`。
- `git diff --check`：无空白错误；存在 Git 既有的 LF/CRLF 提示。
- 后端、API 文件、session provider、face-analysis reducer 的 diff 为空。结果展示模型增加的是已有数据的排版映射，没有修改请求结构。

浏览器实渲染检查：18 张当前页面截图，包括首页 320/375/390/430 宽度，结果/产品/恢复页 320 与 390 宽度，以及历程、区域详情、产品详情、新增产品、账户和登录。

- `artifacts/ui-rebuild-review/index.html`：可点击截图预览。
- `artifacts/ui-rebuild-review/review-results.json`：实际路由、尺寸、溢出与跳过原因。
- 18 次截图均未检测到文档横向溢出；结果页 320 宽照片与文字重叠的浏览器回归检查已通过。
- 所有测试账号、图片、记录和 API 响应均为隔离 fixture。未连接后端完成真实登录、AI 分析或保存操作，不属于后端联调通过证据。

重跑方式：在 `mobile` 启动 `npx expo start --web --port 8082`，另一个终端运行 `node scripts/ui-visual-review.mjs`。脚本使用本机 Chrome、临时独立用户目录、端口 8000 的 API 请求拦截。API 地址或端口改变时，应先同步脚本的拦截范围。没有安装浏览器测试依赖。

## 原生验收待办

当前环境没有可用的 Android SDK/ADB 或 iPhone 连接。Web 的 Expo File 构造函数调用 `validatePath()`，而 SDK 57 Web 实现没有该方法，因此选图后进入现有错误恢复页。没有为绕过这一限制修改应用或 SDK。Web 照片确认与实时相机明确标记为跳过，不算通过。

下一轮需要连接 Android 和 iPhone，检查：

1. 中文衬线回退、系统放大字体、长产品名与长原话。
2. 相机权限拒绝/重新授权、实时预览、单椭圆位置、快门和返回。
3. 相册原图进入确认、选择 3 个以上区域、历史选择完整恢复、必检区域不可取消。
4. 原话输入时的键盘避让、安全区与底部按钮可达性。
5. 质量失败、重试、分段确认、分析完成后的原始照片与详细事实。
6. 产品左滑归档、系统减少动态效果、返回后的列表状态。

本轮没有数据库迁移、依赖安装、真实记录删除或 Git 集成操作。
